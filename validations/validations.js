//validation arrays go here and imported to router function

//empty example exported to default/defaultRouters.js
import {body , query , param} from 'express-validator'
const example = [
    //use express validator 
    //body('some input).followed by other validations
]

export{
    example,
};